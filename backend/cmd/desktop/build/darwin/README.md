# macOS Build Notes

This folder is reserved for future macOS Wails migration work.

Current plan boundary:

- `apps/macos` remains the active macOS desktop implementation
- this directory is documentation-only scaffolding
- no macOS Wails build is being activated from here
